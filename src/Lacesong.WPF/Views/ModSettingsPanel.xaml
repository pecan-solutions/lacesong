<UserControl x:Class="Lacesong.WPF.Views.ModSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="300">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Mod Settings" FontSize="20" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <CheckBox Content="Enable automatic updates" IsChecked="{Binding AutoUpdateEnabled}" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Update channel:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Width="120" SelectedItem="{Binding UpdateChannel}">
                    <ComboBoxItem Content="stable"/>
                    <ComboBoxItem Content="beta"/>
                    <ComboBoxItem Content="alpha"/>
                </ComboBox>
            </StackPanel>
            <CheckBox Content="Preserve/merge config files" IsChecked="{Binding PreserveConfigs}" Margin="0,0,0,8"/>
            <CheckBox Content="Create backup before update" IsChecked="{Binding BackupBeforeUpdate}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button Content="Save" Width="80" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
