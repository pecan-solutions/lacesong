<ui:FluentWindow x:Class="Lacesong.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Lacesong.WPF.ViewModels"
        xmlns:views="clr-namespace:Lacesong.WPF.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Lacesong - Mod Manager" 
        Height="720" 
        Width="1080"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <ui:TitleBar Grid.Row="0" Title="Lacesong" ShowMaximize="False" ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/Resources/icon.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <ui:NavigationView x:Name="RootNavigation" Grid.Column="0"
                               PaneDisplayMode="Left"
                               Transition="Slide">
                <ui:NavigationView.MenuItems>
                    <ui:NavigationViewItem Content="Home" Icon="Home24" TargetPageType="{x:Type views:HomeView}" />
                    <ui:NavigationViewItem Content="Game Detection" Icon="Games24" TargetPageType="{x:Type views:GameDetectionView}" />
                    <ui:NavigationViewItem Content="Mod Loader" Icon="Box24" TargetPageType="{x:Type views:BepInExInstallView}" />
                    <ui:NavigationViewItem Content="Mods" Icon="Apps24" TargetPageType="{x:Type views:ModCatalogView}" />
                </ui:NavigationView.MenuItems>
                <ui:NavigationView.FooterMenuItems>
                    <ui:NavigationViewItem Content="Settings" Icon="Settings24" TargetPageType="{x:Type views:SettingsView}" />
                </ui:NavigationView.FooterMenuItems>
            </ui:NavigationView>

            <!-- Content Area -->
            <Border Grid.Column="1" Background="Transparent" CornerRadius="8,0,0,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <ContentPresenter x:Name="ContentArea" Grid.Row="0"
                                      Content="{Binding ElementName=RootNavigation, Path=CurrentPage}" />

                    <!-- Status Bar -->
                    <Border Grid.Row="1" Background="{DynamicResource StatusBarBrush}" 
                            BorderBrush="{DynamicResource GlassBorderBrush}" 
                            BorderThickness="0,1,0,0"
                            Padding="16,8"
                            CornerRadius="0,0,8,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" FontSize="12"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text=" | " VerticalAlignment="Center" Margin="8,0" FontSize="12"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock Text="{Binding CurrentGame.Name, StringFormat='Game: {0}'}" VerticalAlignment="Center" FontSize="12"
                                           Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding CurrentGame.InstallPath}" VerticalAlignment="Center" FontSize="11"
                                           Foreground="{DynamicResource MutedTextBrush}" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="CheckmarkCircle24" 
                                               Foreground="{DynamicResource SuccessBrush}"
                                               Visibility="{Binding HasError, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                <ui:SymbolIcon Symbol="ErrorCircle24"
                                               Foreground="{DynamicResource ErrorBrush}"
                                               Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding IsBusy, Converter={StaticResource BusyStatusConverter}}" FontSize="11"
                                           VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
