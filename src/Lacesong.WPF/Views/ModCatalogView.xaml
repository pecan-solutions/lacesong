<UserControl x:Class="Lacesong.WPF.Views.ModCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Lacesong.WPF.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:ModCatalogViewModel}">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Mod Management" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8"/>
            <TextBlock Text="Manage your installed mods and discover new ones." 
                       Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search and Filters -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBox Style="{StaticResource SearchTextBoxStyle}" Text="{Binding SearchText}" Width="200" MinWidth="150" Margin="0,0,12,0"/>
                    <CheckBox Style="{StaticResource DefaultCheckBoxStyle}" Content="Installed Only" IsChecked="{Binding ShowInstalledOnly}" Margin="0,0,12,0"/>
                    <CheckBox Style="{StaticResource DefaultCheckBoxStyle}" Content="Enabled Only" IsChecked="{Binding ShowEnabledOnly}" />
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Install from File" Command="{Binding InstallModFromFileCommand}" Margin="0,0,8,0" MinWidth="120"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Install from URL" Command="{Binding InstallModFromUrlCommand}" Margin="0,0,8,0" MinWidth="120"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Refresh" Command="{Binding RefreshModsCommand}" Margin="0,0,8,0" MinWidth="80"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Open Mods Folder" Command="{Binding OpenModsFolderCommand}" Margin="0,0,8,0" MinWidth="140"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Settings" Command="{Binding OpenModSettingsCommand}" MinWidth="80"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Mod List -->
        <Grid Grid.Row="2">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding InstalledMods}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,16,16" MinWidth="300" MaxWidth="350">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource SectionHeaderTextBlockStyle}" Margin="0,0,0,12"/>
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource SmallFadedTextBlockStyle}"
                                               TextWrapping="Wrap" Margin="0,0,0,8" MaxHeight="60" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Author, StringFormat='Author: {0}'}" Style="{StaticResource SmallFadedTextBlockStyle}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Version, StringFormat='Version: {0}'}" Style="{StaticResource SmallFadedTextBlockStyle}" Margin="0,0,0,16"/>

                                    <!-- Action Buttons -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Button Grid.Column="0" Style="{StaticResource SecondaryButtonStyle}" Content="Enable" 
                                                Command="{Binding DataContext.EnableModCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                IsEnabled="{Binding CanBeEnabled}" Margin="0,0,4,0"/>

                                        <Button Grid.Column="1" Style="{StaticResource SecondaryButtonStyle}" Content="Disable"
                                                Command="{Binding DataContext.DisableModCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                IsEnabled="{Binding IsEnabled}" Margin="4,0,4,0"/>

                                        <Button Grid.Column="2" Style="{StaticResource DangerButtonStyle}" Content="Uninstall"
                                                Command="{Binding DataContext.UninstallModCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                IsEnabled="{Binding CanBeUninstalled}" Margin="4,0,0,0"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
