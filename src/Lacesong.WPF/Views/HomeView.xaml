<UserControl x:Class="Lacesong.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Lacesong.WPF.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:HomeViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,32">
            <TextBlock Text="Welcome to Lacesong" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8"/>
            <TextBlock Text="Your all-in-one mod manager for Silksong. Get started below." 
                       Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Quick Actions -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Quick Actions" Style="{StaticResource SectionHeaderTextBlockStyle}" Margin="0,0,0,20"/>
                    
                    <Button Content="Install Mod from File" Command="{Binding InstallModFromFileCommand}" 
                              Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,12"/>
                    
                    <Button Content="Install Mod from URL" Command="{Binding InstallModFromUrlCommand}" 
                              Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,12"/>
                    
                    <Button Content="Browse Mod Catalog" Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                              CommandParameter="{x:Type vm:ModCatalogViewModel}" 
                              Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,16"/>
                    
                    <Separator Margin="0,8,0,16"/>
                    
                    <TextBlock Text="New to Modding?" Style="{StaticResource SmallHeaderTextBlockStyle}" Margin="0,0,0,8"/>
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,4" Text="1. Make sure your game is detected" />
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,4" Text="2. Install the mod loader if needed" />
                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="3. Install your favorite mods" />
                    <Button Content="Launch Modded Game" Command="{Binding DataContext.LaunchModdedCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,12"/>
                    <Button Content="Launch Vanilla Game" Command="{Binding DataContext.LaunchVanillaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,0,12"/>
                </StackPanel>
            </Border>

            <!-- Game Status -->
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Game Status" Style="{StaticResource SectionHeaderTextBlockStyle}" Margin="0,0,0,20"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="16"
                                       Foreground="{DynamicResource SuccessBrush}" 
                                       Visibility="{Binding DataContext.IsGameDetected, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <ui:SymbolIcon Symbol="Warning24" FontSize="16"
                                       Foreground="{DynamicResource WarningBrush}"
                                       Visibility="{Binding DataContext.IsGameDetected, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding DataContext.IsGameDetected, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource GameStatusConverter}}" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}" Margin="8,0,0,0"/>
                    </StackPanel>
                    
                    <TextBlock Text="{Binding DataContext.CurrentGame.Name, RelativeSource={RelativeSource AncestorType=Window}, StringFormat='Game: {0}'}" 
                               Style="{StaticResource BodyTextBlockStyle}" Margin="0,0,0,8"/>
                    
                    <TextBlock Text="{Binding DataContext.CurrentGame.InstallPath, RelativeSource={RelativeSource AncestorType=Window}}"
                               Style="{StaticResource SmallFadedTextBlockStyle}"
                               TextWrapping="Wrap" Margin="0,0,0,16"/>
                    
                    <Button Content="Detect Game" Command="{Binding DataContext.DetectGameCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                              Style="{StaticResource SecondaryButtonStyle}"
                              IsEnabled="{Binding DataContext.IsGameDetected, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBooleanConverter}}"
                              Margin="0,0,0,8"/>
                    
                    <Button Content="Change Game" Command="{Binding DataContext.SelectGameDirectoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                              Style="{StaticResource SecondaryButtonStyle}"
                              IsEnabled="{Binding DataContext.IsGameDetected, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
