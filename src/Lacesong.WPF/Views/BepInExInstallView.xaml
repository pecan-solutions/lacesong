<UserControl x:Class="Lacesong.WPF.Views.BepInExInstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Lacesong.WPF.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:BepInExInstallViewModel}">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Mod Loader Installation" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8"/>
            <TextBlock Text="Install and manage the BepInEx mod loader for Silksong." 
                       Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="280"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*" MinWidth="280"/>
            </Grid.ColumnDefinitions>

            <!-- Status Panel -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Current Status" Style="{StaticResource SectionHeaderTextBlockStyle}" Margin="0,0,0,16"/>
                    
                    <TextBlock Text="{Binding InstallationStatus}" Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" Margin="0,0,0,16"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="✅" FontSize="16"
                                       Foreground="{DynamicResource SuccessBrush}" 
                                       Visibility="{Binding IsBepInExInstalled, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <TextBlock Text="⚠️" FontSize="16"
                                       Foreground="{DynamicResource WarningBrush}"
                                       Visibility="{Binding IsBepInExInstalled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding IsBepInExInstalled, Converter={StaticResource BepInExStatusConverter}}" 
                                   Style="{StaticResource BodyStrongTextBlockStyle}" Margin="8,0,0,0"/>
                    </StackPanel>
                    
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Refresh Status" Command="{Binding RefreshStatusCommand}" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="BepInEx is the mod loader required to use most mods with Silksong. It must be installed before you can load any mods." 
                               Style="{StaticResource SmallFadedTextBlockStyle}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Installation Panel -->
            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Install Options" Style="{StaticResource SectionHeaderTextBlockStyle}" Margin="0,0,0,16"/>
                    
                    <!-- Version Selection -->
                    <TextBlock Text="BepInEx Version" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,0,8"/>
                    <ComboBox Style="{StaticResource DefaultComboBoxStyle}" ItemsSource="{Binding AvailableVersions}" 
                                 SelectedItem="{Binding SelectedVersion}"
                                 Margin="0,0,0,16">
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem" BasedOn="{StaticResource ComboBoxItemStyle}"/>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                    
                    <!-- Options -->
                    <CheckBox Style="{StaticResource DefaultCheckBoxStyle}" Content="Force reinstall (if already installed)" 
                                 IsChecked="{Binding ForceReinstall}"
                                 Margin="0,0,0,8"/>
                    
                    <CheckBox Style="{StaticResource DefaultCheckBoxStyle}" Content="Create backup before installation" 
                                 IsChecked="{Binding CreateBackup}"
                                 Margin="0,0,0,8"/>
                    
                    <CheckBox Style="{StaticResource DefaultCheckBoxStyle}" Content="Create desktop shortcut" 
                                 IsChecked="{Binding CreateDesktopShortcut}"
                                 Margin="0,0,0,20"/>
                    
                    <!-- Action Buttons -->
                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="Install BepInEx" Command="{Binding InstallBepInExCommand}" 
                               IsEnabled="{Binding CanInstall}" Margin="0,0,0,8" MinWidth="160" MinHeight="44"/>
                    
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="Uninstall BepInEx" Command="{Binding UninstallBepInExCommand}" 
                               IsEnabled="{Binding CanUninstall}" MinWidth="160" MinHeight="44"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Progress/Status -->
        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0,16,0,0" 
                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Installation in Progress..." Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,0,0,12"/>
                
                <ProgressBar IsIndeterminate="True" Height="4" />
                
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource SmallFadedTextBlockStyle}"
                           Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
