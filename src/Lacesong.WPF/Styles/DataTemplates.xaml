<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:Lacesong.WPF.ViewModels"
                    xmlns:views="clr-namespace:Lacesong.WPF.Views">

    <DataTemplate DataType="{x:Type vm:HomeViewModel}">
        <views:HomeView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:GameDetectionViewModel}">
        <views:GameDetectionView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:BepInExInstallViewModel}">
        <views:BepInExInstallView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ModCatalogViewModel}">
        <views:ModCatalogView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
        <views:SettingsView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:BrowseModsViewModel}">
        <views:BrowseModsView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:GameNotSelectedViewModel}">
        <Border Padding="24">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="No game selected" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center" />
                <TextBlock Text="Please detect or choose your Silksong installation before browsing mods." Style="{StaticResource SmallFadedTextBlockStyle}" TextWrapping="Wrap" Width="400" TextAlignment="Center" Margin="0,12,0,0" />
            </StackPanel>
        </Border>
    </DataTemplate>

</ResourceDictionary>
